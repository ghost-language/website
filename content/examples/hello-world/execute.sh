# To run the program, put the code in
# `hello-world.ghost` and use the interpreter.
$ ghost hello-world.ghost
hello world

# Now that we can run basic Ghost programs, let's
# learn more about the language.